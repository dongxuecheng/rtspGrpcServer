version: '3.8'
services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    ports:
      - "8081:8080"
    environment:
      SWAGGER_JSON: /app/swagger.json
    volumes:
      - ./proto/stream.swagger.json:/app/swagger.json